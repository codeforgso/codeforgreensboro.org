# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'

# TODO: change password, username, host, and port to environment variables

development:
  adapter: postgresql
  database: db/dev_cfg
  pool: 5
  timeout: 5000
  password: password
  username: postgres
  host: localhost
  port: 8080

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  database: db/test_cfg
  pool: 5
  timeout: 5000
  password: password
  username: postgres
  host: localhost
  port: 8080

production:
  adapter: postgresql
  database: db/prod_cfg
  pool: 5
  timeout: 5000
  password: password
  username: postgres
  host: localhost
  port: 8080
